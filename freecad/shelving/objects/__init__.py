"""FreeCAD scripted-object layer for the Shelving Workbench.

Holds the `Plank` `Part::FeaturePython` proxy, its geometry seam
(`geometry.plank_shape`), and the generated-`Label` mapping (`labels`). The
container object that owns a tree of these planks arrives in sh-012.
"""
