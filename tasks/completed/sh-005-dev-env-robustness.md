---
id: sh-005
title: "Dev-environment robustness: editable pixi install, venv-drift preflight"
current_agent: user
current_phase: done
review_rejections: 1
---

# sh-005: Dev-environment robustness: editable pixi install, venv-drift preflight

## Summary
Two dev-environment papercuts from sh-003/sh-004: the pixi environment has no
editable `shelving_core` install, so every repo-root script needs a hand-copied
`sys.path` bootstrap; and `./test.sh --fast` fails at collection (rather than
with a clear message) when an existing `.venv` is missing a dependency newly
added to the `dev` extra. This task installs `shelving_core` editable into the
pixi env and adds a dev-extra check to the fast-tier preflight.

## Status
- [x] Planning
- [x] Implementation
- [x] Review
- [x] User sign-off

## Must Have
- [x] `pixi.toml` installs the `shelving-workbench` project itself as an editable local package into the pixi environment (`[pypi-dependencies]` with `{ path = ".", editable = true }`, or the equivalent pixi manifest form), so `import shelving_core` works under `pixi run` / `pixi shell` with no per-script path shim. `pixi.lock` is regenerated by `pixi install` and still resolves for `linux-64` and `linux-aarch64`.
- [x] `tools/layout_demo.py` no longer carries a `sys.path` bootstrap: the `_REPO_ROOT` computation and the `if _REPO_ROOT not in sys.path: sys.path.insert(...)` block are removed. Its module docstring states it must run inside an activated environment (`source .venv/bin/activate`, `pixi shell`, or via `pixi run`).
- [x] `tools/freecad_smoke.py` is unchanged. Its bootstrap solves a different problem (importing `freecad.shelving` from the checkout under `freecadcmd`, whose bundled Python does not get pixi's editable install) and stays.
- [x] `./test.sh --fast`'s `preflight_fast` gains a check: read the distribution names in `pyproject.toml`'s `[project.optional-dependencies].dev` (via `tomllib`, standard library) and confirm each is installed (`importlib.metadata.distribution(name)` succeeds). If one or more are missing, print `dev environment is out of sync with the [dev] extra: <names>. Run tools/install-deps.sh.` to stderr and exit 3 (same code as the missing-tool case). No network access.
- [x] The dev-extra check runs before the existing tool checks or right after them, but always before `ruff`/`mypy`/`pytest` are invoked, so a stale env produces one clear message instead of a pytest collection error.
- [x] `./test.sh --fast` exits 0 in a synced environment; `pixi run full` is green; `pixi run demo` and (from an activated `.venv`) `python tools/layout_demo.py` both exit 0; `tests/test_layout_demo.py` still passes without the removed bootstrap.
- [x] `README.md` Setup or Tests section notes that repo-root scripts (`tools/layout_demo.py`) require an activated environment.
- [x] Both `2026-08-31` friction-log entries (repo-root import bootstrap; test-only dep not reaching a provisioned `.venv`) are removed from `.claude/docs/friction-log.md`.
- [x] `mypy --strict` and `ruff check .` / `ruff format --check .` clean. No new third-party dependency (the editable self-install is the project, not a dependency).

## Frontier Advice

STANDING OBLIGATIONS (`CLAUDE.md`): **Typed Python** applies. If the preflight
check is a `.py` helper it is fully typed; if it is an inline `python -c "..."`
inside `test.sh`, keep it minimal and correct (a short script reading
`tomllib.load` and iterating `importlib.metadata.distribution`). No other
standing obligation is active.

PIXI EDITABLE INSTALL: pixi supports a local editable package via
`[pypi-dependencies]` `shelving-workbench = { path = ".", editable = true }`.
Confirm the exact key against the installed pixi 0.78 (`pixi add --help` /
`pixi add --pypi --editable .` may be the ergonomic path). After editing the
manifest run `pixi install`, then `pixi run python -c "import shelving_core;
print(shelving_core.__file__)"` and confirm it points at the checkout, not a
copy. Commit the updated `pixi.lock`.

PREFLIGHT CHECK: distribution name is the PyPI/`pyproject` name, so
`importlib.metadata.distribution("jsonschema")` is the right call, not an
`import jsonschema`. This also works in the pixi (conda) env, where conda
registers dist metadata. Do not shell out to `pip`; `pip check` does not detect
a missing extra. Keep it network-free and fast.

`test.sh` STAYS AUTHORITATIVE for tier contents. The new check is part of the
`--fast` preflight only; `--full` inherits it because `--full` runs the full
`--fast` sequence. Exit code 3 stays reserved for "dev environment not ready"
(missing tool OR missing dev-extra dist); 2 for usage.

FRICTION LOG: this task resolves both `2026-08-31` entries; delete them in the
same commit that lands the fixes, per `.claude/docs/friction-log.md`'s protocol.
Record any NEW workaround per `CLAUDE.md`.

## Execution Plan

- [x] **Step 1** (`pixi.toml`, `pixi.lock`): Add the editable local install of `shelving-workbench` to the pixi environment per PIXI EDITABLE INSTALL. Run `pixi install`; verify `pixi run python -c "import shelving_core"` resolves to the checkout; commit the regenerated `pixi.lock` (both platforms).

- [x] **Step 2** (`test.sh`): Extend `preflight_fast` with the dev-extra distribution check per the Must Have and PREFLIGHT CHECK: read `[project.optional-dependencies].dev` from `pyproject.toml` with `tomllib`, check each with `importlib.metadata.distribution`, exit 3 with the named-missing message pointing at `tools/install-deps.sh`. Runs before `ruff`/`mypy`/`pytest`.

- [x] **Step 3** (`tools/layout_demo.py`): Remove the `_REPO_ROOT` / `sys.path.insert` bootstrap. Update the module docstring to state the activated-environment requirement. Leave everything else (argparse `--svg`, the text dump, `rule_label` import) unchanged.

- [x] **Step 4** (`README.md`): Note that repo-root scripts like `tools/layout_demo.py` require an activated environment.

- [x] **Step 5** (`.claude/docs/friction-log.md`): Delete the two `2026-08-31` entries.

- [x] **Step 6** (verification, no new files): `./test.sh --fast` green; `pixi run full` green; `pixi run demo` and `python tools/layout_demo.py` (in an activated `.venv`) exit 0; `pytest tests/test_layout_demo.py` passes. Simulate a stale env (a `PATH`/venv missing `jsonschema`) and confirm the preflight exits 3 naming it.

## Round 2 Notes (review-rejection round 1)

- **F1 resolved.** pixi 0.78 CAN write a repo-relative path for a `[pypi-dependencies]` path entry. The absolute `- pypi: /workspace` was an artifact of the incremental lock `pixi add --editable .` seeded. `pixi lock` with the existing lock present treats it as "already up-to-date" and keeps the absolute string; deleting the lock and regenerating from a clean state (`rm pixi.lock && pixi lock`) rewrites all three references to `- pypi: ./`. The clean regen also refreshed ~a dozen conda packages to current conda-forge builds (build-number bumps plus `zizmor` 1.29.0 -> 1.30.0) for both `linux-64` and `linux-aarch64`; both tiers pass on the refreshed lock. Hand-editing the three lines was rejected as fragile (would invalidate pixi's lock content hash and re-trigger a solve anyway).
- **F1 guard:** `tools/check_lock_paths.py` (typed, `mypy --strict`-clean) scans `pixi.lock` for any package-location key pinned to an absolute path or `file://` URI; `test.sh`'s `run_fast` invokes it as `preflight_lock_paths` before ruff/mypy/pytest, exiting 1 with the offending lines. Covered by `tests/test_check_lock_paths.py`, including a case asserting the committed `pixi.lock` is clean.
- **F2 resolved** via the helper route: the `preflight_dev_extra` heredoc moved into `tools/check_dev_extra.py` (typed, `mypy --strict`-clean); `test.sh` calls it. `tests/test_check_dev_extra.py` unit-tests name extraction (specifiers, markers, extras, `@` direct refs), missing-distribution detection in input order, and pins the exact message `dev environment is out of sync with the [dev] extra: <names>. Run tools/install-deps.sh.` with exit status 3, both via `main()` and via a subprocess CLI call.
- **N1:** `python3` added to both `preflight` tool lists (`--fast` and `--full`) and to `FAST_TOOLS` in `tests/test_harness_cli.py`.
- **N2:** `@` added to the requirement-name split class, now `_NAME_END = re.compile(r"[<>=!~;\[@\s]")` in `tools/check_dev_extra.py`.

- [x] **Step 7** (`tools/check_dev_extra.py`, `test.sh`, `tests/test_check_dev_extra.py`, `pyproject.toml`): F2 + N2. Extract the dev-extra check to a typed helper, wire `test.sh` to it, add the helper to mypy `files`, unit-test message and exit code.
- [x] **Step 8** (`tools/check_lock_paths.py`, `test.sh`, `tests/test_check_lock_paths.py`, `pyproject.toml`, `pixi.lock`): F1. Regenerate `pixi.lock` from clean so the self-install pins `./`; add the typed absolute-path guard, wire it into `run_fast`, add to mypy `files`, test it.
- [x] **Step 9** (`test.sh`, `tests/test_harness_cli.py`): N1. Add `python3` to both preflight tool lists and to `FAST_TOOLS`.
- [x] **Step 10** (`.claude/docs/friction-log.md`): add the Bash-tool-PATH entry at the top of `## Entries` (user-requested this round).
- [x] **Step 11** (verification): re-run both tiers, the demo two ways, the layout-demo test, the two new test modules, the stale-env and absolute-lock simulations; `mypy --strict` / `ruff` clean.
